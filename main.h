#ifndef _main_h
#define _main_h

#include <stdlib.h>

int _printf(const char *format, ...);
int _putchar(char c);
int putint(int n);
int to_binary(unsigned int num);
int to_octal(unsigned int num);
int put_unsign(int num);
int to_Hex(unsigned int num);
int to_hex(unsigned int num);

#endif
